<Window x:Class="IPFS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IPFS"
        mc:Ignorable="d"
        DataContext="{Binding MainWindowVM,Source={StaticResource ServiceLocator}}"
        Title="MainWindow" Height="720" Width="1024"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>
            
            <Grid.Background>
                <ImageBrush ImageSource="/Resources/Image/autumn.jpg" Stretch="UniformToFill" Opacity="0.2"/>
            </Grid.Background>

            <TextBlock Text="WPF" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22"/>

            <StackPanel Grid.Row="1">
                <Button Margin="15 0 15 0" >
                    <StackPanel Orientation="Horizontal">
                        <TextBlock/>
                        <TextBlock Text="Bing"/>
                    </StackPanel>
                </Button>
            </StackPanel>

        </Grid>
        
        
    </Grid>
</Window>
